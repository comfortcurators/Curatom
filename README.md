# Curatom
A shared memory and registry for multi-agent fleets that doesn’t serve every LLM the same context the same way. Curatom profiles each model’s native format, retention window, and accuracy tolerance — Gemini, Llama, Nemotron, etc. — and reshapes what it recalls to match how that specific model actually reads and remembers.
